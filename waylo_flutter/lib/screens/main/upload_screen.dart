import 'package:flutter/material.dart';

class UploadScreenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('업로드 페이지'),
        ),
      ),
    );
  }
}
